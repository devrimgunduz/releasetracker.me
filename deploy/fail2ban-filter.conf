# /etc/fail2ban/filter.d/release-radar.conf
# Matches the WARNING line emitted by app/web/auth.py on a failed login.
# The app logs to the journal via systemd, so the jail uses backend=systemd.
[Definition]
failregex = failed login for .* from <HOST>$
ignoreregex =
